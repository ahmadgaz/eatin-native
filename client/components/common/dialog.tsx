/*
open state
loading state
disabled state  
title (if text, render as text, else render as children)
subtitle (if text, render as text, else render as children)
footer (if text, render as text, else render as children)
onClose
onPress
accessibliityLabel
focus management (When the dialog opens, the focus should move to it. When it closes, the focus should return to where it was before the dialog opened.)
*/
