/*
visible state
loading state
duration
message (if text, render as text, else render as children)
optional action (if text, render as text btn, else render as children)
onClose
accessibliityLabel
*/
